#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

if [[ $# -gt 0 && "$1" == skills/* && ! -e "$1" && -e ".agents/$1" ]]; then
  set -- ".agents/$1" "${@:2}"
fi

if [[ -x .venv/bin/python3 ]]; then
  exec .venv/bin/python3 "$@"
fi
exec python3 "$@"
